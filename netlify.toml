[build]
  command = "mkdir -p public/app public/cliente && cp admin.html public/index.html && cp admin.html public/admin.html && cp index.html public/app/index.html && cp index.html public/cliente/index.html && cp juegos.json config.json public/ || true"
  publish = "public"
